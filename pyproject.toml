[project]
name = "elevator-queue-lab"
version = "0.1.0"
description = "Queueing-theory playground for high-rise elevator dispatch policies"
requires-python = ">=3.11"

[project.scripts]
elevator-queue-lab = "app.server:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

